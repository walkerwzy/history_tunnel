实现 scrape_civilizations_timeline 方法，具体实现如下：
1. 搜索策略：
- 英文维基百科搜索
- 中文LLM缓存和数据库
2. 处理流程：
- 搜索 → 获取英文页面 → 用写好的llm processor处理（但写好后注释掉）， 直接提取结构化事件 → 保存中文LLM缓存 → 检查数据库去重 → 插入
3. 时间边界：
- 到第一次世界大战（1914年之前）
4. 限制：
- 每个时期不超过20个事件

检查代码没有问题后就开始抓取，特别注意的是从维基内容到结构化事件的部分，是不是写好了代码并注释掉，以及执行过程中要你自己来帮我做到这一部分。目的是既然你已经是个大模型能帮我做到，我没必要再连接另一个大模型。
